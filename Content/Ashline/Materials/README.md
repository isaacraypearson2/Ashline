# Materials

```
PBR/     master + instances after you author them
Decals/M_Impact_Bullet
Cosmetics/M_{CosmeticId}   ← locker MaterialOverride
```

Until then the builder tints Engine `DefaultMaterial` / `WorldGridMaterial` / Starter Content surfaces (`M_Ground_Grass`, `M_Concrete_Tiles`, `M_Metal_Steel`, `M_Water_Ocean`, `M_Rock_Marble`).
