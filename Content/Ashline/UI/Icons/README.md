# Locker preview icons

Convention (catalog `PreviewIconPath`):

```
T_{CosmeticId}
T_{SkinId}
T_{WeaponId}          also T_WPN_*
T_{EquipmentId}
```

Soft-ref only. Missing icons never crash. Canvas HUD does not require these.
